//! `tars-examples` — a home for the workspace's runnable demos.
//!
//! There is no library API here. Each demo lives under `examples/` and is run
//! via `cargo run -p tars-examples --example <name>` (e.g. `non_llm_plan`,
//! `multi_step_with_tools`, `blackboard_demo`, `testing`).
