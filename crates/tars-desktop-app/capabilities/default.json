{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default capability for the main window — core commands only (our own invoke handlers need no extra permissions).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
